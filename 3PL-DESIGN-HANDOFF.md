# 株式会社ハシゴロジ 3PL事業ページ ─ デザイン引き継ぎ資料

作成日：2026年8月11日
対象ページ：`https://hashigologi.com/3pl/`（未公開／制作中）
リポジトリ：`https://github.com/takayuda/hashigo-logi-hp`
ブランチ：`claude/3pl-business-page-zhpjup`

このページは**たたき台**です。構成と原稿は固まっていますが、ビジュアルの精度は上げきれていません。
写真も6か所が未手配で、枠だけ用意した状態です。ブラッシュアップをお願いします。

---

## 1. まず見る

```
リポジトリのルートで
  python3 -m http.server 8000
ブラウザで
  http://localhost:8000/3pl/
```

トップページは `http://localhost:8000/` です。
`3pl/index.html` を直接ダブルクリックすると、`../style.css` の相対パスが解決できず
レイアウトが崩れます。必ず上のようにサーバー経由で開いてください。

Cloudflare Workers でプレビューURLを発行して共有することもできます。

```
npx wrangler versions upload
```

本番URLは変わらず、プレビュー専用のURLが発行されます。

---

## 2. ファイル構成

| ファイル | 役割 |
|---|---|
| `3pl/index.html` | **3PLページ本体。編集対象はここ** |
| `style.css` | サイト全体で共有。3PLページ用は「事業詳細ページ」以降のブロック |
| `script.js` | ヘッダーの挙動、ハンバーガーメニュー、スクロール表示 |
| `index.html` | トップページ。事業カードとフッターから `/3pl/` にリンク |
| `IMG-*.webp` / `IMG-*.jpg` | 写真。`.webp` が配信用、`.jpg` がマスター |
| `.assetsignore` | 配信対象から外すファイル。マスター画像と `*.md` を除外 |

配布用にまとめたzip（`3pl-preview.html` 同梱・1ファイルで開ける版）は別途お渡ししています。

---

## 3. デザインの決まりごと

既存サイト（トップページ）から引き継いでいるルールです。
**変更を提案していただくのは歓迎です**が、変える場合はトップページも合わせて動かす必要があります。

### 色

`style.css` 冒頭の `:root` で定義しています。ここを書き換えるとサイト全体に反映されます。

| 変数 | 値 | 用途 |
|---|---|---|
| `--navy` | `#0b2b4d` | 見出し、濃い面、テーブルヘッダ |
| `--navy-d` | `#07203a` | フッター、メニュー背景 |
| `--blue` | `#0b63c5` | アクセント。ラベル、ボタン、強調文字 |
| `--blue-d` | `#0a539f` | ボタンのホバー |
| `--ink` | `#1a1f26` | 本文 |
| `--gray` | `#5b636e` | 補足文 |
| `--gray-l` | `#8a919b` | 注記、非活性 |
| `--line` | `#e2e6ec` | 罫線・枠線（1px） |
| `--bg` / `--bg-g` | `#ffffff` / `#f5f7fa` | 背景。セクションごとに交互 |

濃い背景の上では `--eyebrow-w`（`#8fc4f5`）を小見出しに使っています。

### タイポグラフィ

- 和文：**Noto Sans JP**（400 / 500 / 700 / 900）
- 欧文・数字：**Barlow**（600 / 700）。`letter-spacing` を広め（.14〜.22em）に取り、
  小見出しラベル（`SHIPPING FEE` など）と大きな数字に使用
- 本文：15.5px / `line-height` 1.95 / `letter-spacing` .02em
- 見出し `.h2`：`clamp(24px, 3.4vw, 34px)`

数字（550、1,000）は Barlow で大きく出すのがこのページの核です。ここは維持してください。

### レイアウト

- コンテナ最大幅 `--maxw: 1160px` ＋ 左右 24px
- セクション上下 104px（SP 66px）
- ブレークポイント **1000px** と **760px**
- **角丸は使っていません**（`border-radius: 0`）
- 影も原則なし。カードのホバー時のみ `0 14px 34px rgba(11,43,77,.11)`
- 枠線は 1px。装飾より余白と罫線で構造を出す方針

### アニメーション

`.rv` クラスが付いた要素は、画面に入るとフェードインします（`script.js` の IntersectionObserver）。
`prefers-reduced-motion` 指定時は無効になります。要素を追加する場合は `.rv` を付けてください。

---

## 4. ページ構成とコンポーネント

上から順に、対応するクラス名です。

| # | セクション | 主なクラス | 備考 |
|---|---|---|---|
| 1 | メインビジュアル | `.lp-mv` | 背景写真＋紺のグラデ。見出しは 900 weight |
| 2 | 数字の帯 | `.numstrip` | 550円 / 1,000件 / 0円 の3本柱 |
| 3 | ページ内アンカー | `.lp-anchor` | SPでは2列に折り返し |
| 4 | お悩み | `.worry` `.worry-list` | 写真＋チェック6項目。下に `.arw`（下向き三角） |
| 5 | 選ばれる理由 | `.reason` | 写真＋テキストの交互。偶数番は写真が右 |
| 6 | データの流れ | `.sysflow` | 3カラム。中央（`.on`）が紺ベタ。SPでは縦積み＋下向き矢印 |
| 7 | 対応できる作業 | `.opscard` `.chips` | 写真カード3枚＋タグ列 |
| 8 | 料金 | `.price-big` `.ptbl` | 巨大な550＋内訳テーブル |
| 9 | 導入の流れ | `.flow.f4` | 4ステップ |
| 10 | FAQ | `.faq` | `<details>` によるアコーディオン。JS不要 |
| 11 | CTA | `.cta` | 背景写真＋紺の被せ |
| 12 | 関連事業 | `.rel` | 他2事業への導線 |

`.sec-head.ctr` は見出しの中央寄せ、`.sec-g` はグレー背景のセクションです。

---

## 5. 写真について（最重要）

**6か所が未手配です。** 該当箇所には斜線グレーの枠（`.ph`）を置き、
「どんな写真を入れるか」を枠内に書いてあります。

### 差し替え方

該当の

```html
<div class="ph" role="img" aria-label="写真は準備中です">
  <b>PHOTO</b><span>梱包作業の手元／送り状を貼る場面</span>
</div>
```

を、そのまま

```html
<img src="../packing.webp" alt="梱包作業の手元。作業者が段ボールに送り状を貼っている"
     width="1400" height="880" loading="lazy" decoding="async">
```

に置き換えるだけです。親要素側で `object-fit: cover` が効いているので、
サイズ調整のCSSは不要です。`width` / `height` は元画像の実寸を入れてください
（レイアウトのガタつき防止に使っています）。

### 必要な写真の一覧

| 場所 | 表示サイズ（PC） | 推奨素材 | 被写体 |
|---|---|---|---|
| メインビジュアル背景 | 全幅 | 1920×1080 以上 | 倉庫内観。**暗く落としても成立する画**（紺を80〜90%被せます） |
| お悩み | 約 440×352 | 1200×960 | 段ボールに囲まれた事務所で発送作業に追われている様子 |
| 理由01 低価格 | 約 530×326 | 1400×880 | 梱包作業の手元／送り状を貼る場面 |
| 理由02 柔軟性 | 約 530×326 | 1400×880 | ラベル貼り、ギフト包装などの流通加工 |
| 理由03 小ロット | 約 530×326 | ─ | **`IMG-02.webp` を使用中**（差し替え不要） |
| 理由04 システム | 約 530×326 | 1400×880 | タブレット・PCで在庫データを見ている場面。**現在は `IMG-07.webp` を暫定流用**。トップページと重複しているので差し替え推奨 |
| 対応できる作業 ×3 | 約 355×186 | 1000×540 | ①ラベル・シールを貼る手元 ②ギフト包装・リボン掛け ③セット組・詰め合わせ |
| CTA背景 | 全幅 | 1920×1080 以上 | 倉庫内観。**暗く落としても成立する画** |

- 形式は **WebP**（品質80前後）。既存の写真も同じ方針です
- 元画像（.jpg）はリポジトリに残し、配信は .webp を使う運用にしています
- `alt` は必ず入れてください。「何が写っているか」を具体的に書く方針で統一しています
- 背景写真（メインビジュアル・CTA）だけは `style.css` の `--img-*` 変数で指定しています。
  差し替える場合は **`3pl/index.html` の `<link rel="preload">` も同じURLに揃えてください**

現在、背景写真は暫定でUnsplashのフリー素材を直接参照しています。本番用の写真に差し替える前提です。

---

## 6. 触るときの注意

- **料金の数字は4か所にあります。**メインビジュアル、数字の帯、料金セクション、
  そして `<head>` 内の構造化データ（JSON-LD）。**変更するときは必ず全部揃えてください。**
  HTMLにもコメントを入れてあります
- **FAQの文言も構造化データと同期しています。**表示テキストを変えたら
  `<head>` の `FAQPage` 側も同じ文言に直してください。ここがずれるとSEO上の減点対象です
- 見出しの階層（h1 → h2 → h3）は崩さないでください
- `.hp` クラスはお問い合わせフォームのボット除けです（このページにはありません）

---

## 7. 未確定・要検討

デザイン以前に、内容として詰まっていない点です。制作を進める前に共有しておきます。

- **「550円〜/点」の定義**が未確定。1出荷あたりか、商品1点あたりかで表記が変わります
- **導入事例・実績セクションがありません。**参考にした3PL系のLPでは事例が主要な訴求要素です。
  出せる情報が固まり次第、追加する想定です。入れる位置は「対応できる作業」と「料金」の間が有力です
- **連携できるカート／受注管理システムの固有名**を出していません（実績確定後にロゴ列を追加したい）
- 「ご相談・お見積 0円」「最低利用料なし」は事業方針の確認待ちです

---

## 8. 参考にしたLP

構成の参考として以下を挙げていました。文章量を絞り、写真と大きな数字で読ませる方向です。

- イーロジット 化粧品物流LP `https://e-logit.jp/lp/cosmetics/`
- 鈴与 3PL `https://www.suzuyo.co.jp/3pl/`
