# 3PL事業ページ リデザイン ─ 適用記録・設計メモ

**このリデザインは 2026年8月11日にブランチ `claude/3pl-business-page-zhpjup` へ適用済みです。**
以下はデザイナーから受け取った実装指示書の本文です。§1 の「やること」は適用完了しています。
§2〜§8 は現行ページの設計メモ／申し送りとして有効なので、そのまま残しています。

適用時の確認結果と、指示書からの唯一の逸脱：

- トップページ（`/`）とプライバシーポリシー（`/privacy/`）は、PC・SPともに
  適用前後でレンダリング結果がピクセル単位で完全一致することを確認済み
  （`.page-lp` スコープが正しく効いている）
- `3pl/index.html` の `.faq` にあったインライン `style="background:#fff;padding:0 28px;"` は**削除**した。
  同じ指定が `style.css` の `.page-lp .faq` にあり、インライン側が
  760px以下の `padding:0 18px` を打ち消していたため。意図どおりの余白になった
- `3pl-preview.html` はリポジトリには含めていない。ルート直下の `.html` は
  Cloudflare Workers の配信対象になり、`/3pl-preview.html` として
  重複コンテンツが公開されてしまうため。§6 の手順でいつでも再生成できる

---

# 3PL事業ページ リデザイン ─ 実装指示書

対象リポジトリ：`https://github.com/takayuda/hashigo-logi-hp`
対象ページ：`https://hashigologi.com/3pl/`（未公開／制作中）
前提ブランチ：`claude/3pl-business-page-zhpjup`

このディレクトリのファイルは、既存の `3pl/index.html`（たたき台）をリデザインしたものです。
**そのまま既存ファイルを置き換えれば動きます。** 以下は置き換え時の注意点です。

---

## 1. やること

```
3pl/index.html   ← 全面置き換え
style.css        ← 全面置き換え（差分は末尾ブロックのみ。詳細は §3）
script.js        ← 全面置き換え（差分は2か所＋追記。詳細は §4）
3pl-preview.html ← 全面置き換え（上記3つから生成したプレビュー専用ファイル）
```

写真（`IMG-02.webp` / `IMG-07.webp`）とファビコンは既存のものをそのまま使うので、
リポジトリ側の変更は不要です。同梱しているのは動作確認用のコピーです。

**トップページ（ルートの `index.html`）とプライバシーポリシーは変更しません。**
`style.css` は共有ファイルですが、後述のとおり追加分はすべて `.page-lp` 配下に
スコープしてあるので、トップページの見た目は1pxも変わりません。

### 確認方法

```bash
python3 -m http.server 8000
# → http://localhost:8000/3pl/
```

`3pl-preview.html` はダブルクリックでも開けます（CSS・JS・写真を埋め込み済み）。
ただし**生成物なので直接編集しないでください。**
`3pl/index.html`・`style.css`・`script.js` を直したあと、§6 の手順で作り直します。

### 置き換え後のセルフチェック

- [ ] トップページ（`/`）の表示が変わっていないこと
- [ ] プライバシーポリシー（`/privacy/`）の表示が変わっていないこと
- [ ] `/3pl/` で 1000px 未満に縮めると、下部に追従CTAバーが出ること
- [ ] スクロールすると追従アンカーの下線が現在のセクションに移動すること
- [ ] `prefers-reduced-motion: reduce` でフェードインが無効になること
- [ ] 料金の数字「550」が §5 の4か所すべてで一致していること

---

## 2. `3pl/index.html` の構成

上から順に、対応するクラス名です。

| # | セクション | 主なクラス | 備考 |
|---|---|---|---|
| 1 | メインビジュアル | `.lp-mv` | 背景写真＋紺のグラデ。見出しは 900 weight |
| 2 | 数字の帯 | `.facts` | 550円 / 1,000件 / 0円 の3本柱 |
| 3 | 追従アンカー | `.lp-anchor` | `position:sticky`。ヘッダー直下に貼り付く |
| 4 | お悩み | `.worry` `.worry-list` `.turn` | 写真＋チェック6項目 |
| 5 | 選ばれる理由 | `.reason` | 写真＋テキストの交互。偶数番は写真が右 |
| 6 | 対応できる作業 | `.ops` `.chips` `.ops-more` | アイコンカード3枚＋タグ列 |
| 7 | 料金 | `.pricehead` `.price-big` `.ptbl` | 巨大な550＋内訳テーブル |
| 8 | 導入の流れ | `.steps` | 4ステップ |
| 9 | 代表からのひとこと | `.voice` | |
| 10 | FAQ | `.faq` | `<details>` によるアコーディオン。JS不要 |
| 11 | CTA | `.cta` `.cta-mini` | 背景写真＋紺の被せ |
| — | SP用 追従CTA | `.fixcta` | `</footer>` の直後。1000px以下でのみ表示 |

ヘッダー・フルスクリーンメニュー・フッターのマークアップは**既存のまま**です
（ヘッダーCTAの文言だけ「お問い合わせ」→「無料で相談する」に変更）。

`<body>` に **`class="page-lp"`** が付いています。**これがないとスタイルが一切当たりません。**

### 旧たたき台からの削除

以下は旧版にあり、今回のリデザインでは削除しています。復活させないでください。

- 「データの流れ」セクション（`.sysflow`）
- 「その他の事業」セクション（`.rel`）
- 写真プレースホルダ（`.ph`）── 未手配だった6か所は、フリー素材の仮当てか
  アイコンカードへの置き換えで解消済み（§7参照）

---

## 3. `style.css` の差分

**1〜474行目までは既存ファイルと完全に同一です。** 475行目以降の
「事業詳細ページ（3pl/ ほか）」ブロックだけを丸ごと差し替えています。

diff を取るときは以下で確認できます。

```bash
diff <(head -474 style.css) <(head -474 /path/to/old/style.css)   # → 差分なし
```

### スコープの方針（重要）

新しいブロックのセレクタは**すべて `.page-lp` 配下**にスコープしています。

```css
.page-lp .worry{ ... }        /* ○ */
.worry{ ... }                 /* ✕ 絶対にこう書かない */
```

これは `.sec` `.btn` `.eyebrow` `.cta` `.faq` `.chips` `.crumb` `.note` など、
**トップページと共有しているクラスを下層ページ側で上書きしている**ためです。
スコープを外すとトップページが崩れます。**このブロックに規則を追加するときも、
必ず `.page-lp` を先頭に付けてください。**

例外は `:root` に足した4つのトークンだけです（グローバル）。

| 変数 | 値 | 用途 |
|---|---|---|
| `--acc` | `#c8560f` | 行動色。CTA、チェック、ラベル、斜線ハッチング |
| `--acc-d` | `#a5450b` | ホバー |
| `--acc-w` | `#f0a05a` | 濃い背景の上で使う明るい方 |
| `--hatch` | `repeating-linear-gradient(...)` | 斜線。見出し肩とSTEPの上辺 |

既存の `--navy` `--blue` `--ink` などは値も名前も変更していません。
`--ink` `--gray` `--gray-l` `--line` `--bg-g` は、下層ページでだけ
わずかに濃い値に上書きしています（小さい文字のコントラスト確保）。
これも `.page-lp{ ... }` スコープなのでトップページには効きません。

### 引き継いでいるルール

- コンテナ最大幅・セクション余白・ブレークポイント（1000px / 760px）は既存どおり
- **角丸は使っていません**（`border-radius: 0`）
- 影も原則なし。カードのホバー時のみ `0 14px 34px rgba(11,43,77,.11)`
- 枠線は1px。装飾より余白と罫線で構造を出す方針
- 和文 Noto Sans JP、欧文・数字 Barlow。**数字を Barlow で大きく出すのがこのページの核**

Google Fonts の読み込みウェイトが変わっています（Barlow に 800 を追加、400/500 を削除）。
`3pl/index.html` の `<link>` を参照してください。トップページ側は未変更なので、
**サイト全体で統一したい場合はトップページの `<link>` も合わせてください。**

---

## 4. `script.js` の差分

3か所です。フォーム処理（`FORM_ENDPOINT` 以下）は一切触っていません。

**(1) `neverHide` フラグの追加**

```js
var neverHide = document.body.classList.contains('page-lp');
```

**(2) ヘッダーを隠す条件に `!neverHide` を追加**

追従アンカーがヘッダー直下に貼り付くため、ヘッダーが隠れると目次だけが宙に浮きます。
下層ページでは下スクロール時の自動非表示を無効にしています。

**(3) 下層ページ専用の処理を2つ追記**（フォーム処理の直前）

- 追従アンカーのアクティブ表示（IntersectionObserver）
- SP用 追従CTAの出し入れ

どちらも**対象要素がないページでは即 return する**ので、トップページでは何も起きません。

`.rv`（スクロール表示）は既存の処理をそのまま使っています。
要素を追加する場合は `.rv` を付けてください。

---

## 5. 触るときの注意

### 料金の数字は4か所

**メインビジュアル／数字の帯／料金セクション／`<head>` の JSON-LD。**
変更するときは必ず全部そろえてください。HTMLにもコメントを入れてあります。

`meta description` と `og:description` にも金額が入っています。

### FAQの文言は構造化データと同期

表示テキストを変えたら `<head>` の `FAQPage` 側も同じ文言に直してください。
ここがずれるとSEO上の減点対象です。

### 「550円に含まれる作業」の記載は5か所

数字の帯の補足、料金カードの説明文、FAQの回答、JSON-LD の `FAQPage` と `Offer`。
現在はすべて「入荷、ピッキング、検品、梱包、配送」で統一しています。

### そのほか

- 見出しの階層（h1 → h2 → h3）は崩さないでください
- `alt` は必ず入れてください。「何が写っているか」を具体的に書く方針で統一しています
- `.hp` クラスはお問い合わせフォームのボット除けです（このページにはありません）

---

## 6. `3pl-preview.html` の再生成

`3pl/index.html`・`style.css`・`script.js` を編集したら、以下で作り直してください。
（`3pl-preview.html` を直接編集しても本番には反映されません）

```bash
python3 - <<'EOF'
import base64, pathlib
html = pathlib.Path('3pl/index.html').read_text(encoding='utf-8')
css  = pathlib.Path('style.css').read_text(encoding='utf-8')
js   = pathlib.Path('script.js').read_text(encoding='utf-8')
html = html.replace('<link rel="stylesheet" href="../style.css">', '<style>\n'+css+'\n</style>')
html = html.replace('<script src="../script.js"></script>', '<script>\n'+js+'\n</script>')
for name, mime in [('IMG-02.webp','image/webp'), ('IMG-07.webp','image/webp'),
                   ('favicon-32.png','image/png'), ('favicon.png','image/png')]:
    b = pathlib.Path(name).read_bytes()
    html = html.replace('../'+name, f'data:{mime};base64,'+base64.b64encode(b).decode())
html = html.replace('<!DOCTYPE html>\n',
    '<!DOCTYPE html>\n<!-- このファイルはプレビュー専用です。編集しないでください。\n'
    '     3pl/index.html + style.css + script.js から生成しています -->\n')
pathlib.Path('3pl-preview.html').write_text(html, encoding='utf-8')
EOF
```

---

## 7. 写真について

旧たたき台では6か所が未手配（`.ph` プレースホルダ）でした。現状は次のとおりです。

| 場所 | 現在 | 対応 |
|---|---|---|
| メインビジュアル背景 | Unsplash 仮当て | **要差し替え。** 倉庫内観。紺を90%被せるので暗く落としても成立する画 |
| お悩み | Unsplash 仮当て | 要差し替え。段ボールに囲まれ発送作業に追われている様子 |
| 理由01 低価格 | Unsplash 仮当て | 要差し替え。梱包作業の手元／送り状を貼る場面 |
| 理由02 柔軟性 | Unsplash 仮当て | 要差し替え。ラベル貼り、ギフト包装などの流通加工 |
| 理由03 小ロット | `IMG-02.webp` | 差し替え不要 |
| 理由04 システム | `IMG-07.webp` | トップページと重複。差し替え推奨 |
| 対応できる作業 ×3 | ─ | **写真をやめてアイコンカードに変更。**表示サイズ（約355×186）では被写体が読み取れず、写真の情報量が活きないため |
| CTA背景 | Unsplash 仮当て | **要差し替え。** 倉庫内観。暗く落としても成立する画 |

### 差し替え方

```html
<img src="../packing.webp" alt="梱包作業の手元。作業者が段ボールに送り状を貼っている"
     width="1400" height="880" loading="lazy" decoding="async">
```

親要素側で `object-fit: cover` と `aspect-ratio` が効いているので、サイズ調整のCSSは不要です。
`width` / `height` は元画像の実寸を入れてください（レイアウトのガタつき防止に使っています）。

- 形式は **WebP**（品質80前後）。元画像（.jpg）はリポジトリに残し、配信は .webp を使う運用
- 背景写真（メインビジュアル・CTA）だけは `style.css` の `--img-lp-3pl` / `--img-cta` で指定
- **背景写真を差し替えるときは `3pl/index.html` の `<link rel="preload">` も同じURLに揃えること**
  （LCP要素なので、ずれると計測値が悪化します）

---

## 8. 未確定・要検討（実装前に確認が必要）

デザイン以前に、内容として詰まっていない点です。**公開前に必ず解消してください。**

### 8-1. 「550円」の単位が2種類ある ★最優先

- メインビジュアル：**「550円〜／出荷」**
- 数字の帯・料金セクション・料金表・JSON-LD：**「550円〜／点」**

同一ページ内で単位が食い違っています。読み手は「3点入りの注文は1,650円か?」と必ず引っかかります。
**どちらかに統一してください。** 決まり次第、§5 の4か所＋料金表＋メタを揃える必要があります。

### 8-2. 資材費の記載がない

料金表から「資材費・配送運賃」の行を削除したため、**段ボール・緩衝材の費用が
別途かかるのかどうかがページ上のどこにも書かれていません。**
別途請求する運用なら、料金表に1行戻すか、550円に含める旨を明記してください。
問い合わせ後に「聞いていない」となりやすい項目です。

### 8-3. 導入事例・実績セクションがない

参考にした3PL系のLPでは事例が主要な訴求要素です。出せる情報が固まり次第、
**「導入の流れ」の直後**（`<!-- ▼要確認：導入事例セクションの入る位置 -->` にコメントあり）に追加する想定です。
暫定の穴埋めとして「代表からのひとこと」を置いています。

### 8-4. 代表プロフィールの記載

`.voice` セクションの肩書き・経歴の表現について、事実関係と表記の確認をお願いします。
HTMLに `<!-- ▼要確認 -->` コメントを入れてあります。

### 8-5. 連携できるカート／受注管理システムの固有名

現在は出していません。実績確定後にロゴ列を追加したい旨、旧引き継ぎ資料に記載がありました。

### 8-6. ドメインの不一致

`script.js` の `FALLBACK_MAIL` が `takayuda@hashigo-logi.com`、
`canonical` と OGP は `hashigologi.com` です。どちらかが誤りだと思われます。

---

## 9. 参考にしたLP

構成の参考。文章量を絞り、写真と大きな数字で読ませる方向です。

- イーロジット 化粧品物流LP `https://e-logit.jp/lp/cosmetics/`
- 鈴与 3PL `https://www.suzuyo.co.jp/3pl/`
