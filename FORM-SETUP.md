# お問い合わせフォームのセットアップ

送信内容を **Google スプレッドシートに記録** し、あわせて **メールで通知** します。
サイトは Cloudflare Workers の静的配信のままで、サーバー側のコードは足しません。

```
サイトのフォーム  ──POST──▶  Google Apps Script  ──┬──▶ スプレッドシートに1行追記
                                                  └──▶ 通知メール送信
```

所要 10 分程度です。

---

## 1. スプレッドシートを用意する

1. [Google スプレッドシート](https://sheets.new) で新規ファイルを作成
2. ファイル名を `ハシゴロジ お問い合わせ` などに変更

シートの見出し行はスクリプトが初回送信時に自動で作るので、手で用意する必要はありません。

## 2. Apps Script を設定する

1. そのスプレッドシートで **拡張機能 → Apps Script** を開く
2. エディタの `コード.gs` の中身をすべて消し、`gas/form-handler.gs` の内容を貼り付ける
3. ファイル上部の設定を書き換える

   ```js
   var NOTIFY_TO = 'you@example.com';   // ← 通知を受け取るアドレス
   ```

   - 複数に送る場合はカンマ区切り（`'a@example.com,b@example.com'`）
   - 送信者本人にも自動返信を送りたい場合は `SEND_AUTOREPLY` を `true` に
   - スプレッドシートから作った場合、`SPREADSHEET_ID` は空のままでOK

4. 保存（⌘S / Ctrl+S）

## 3. ウェブアプリとしてデプロイする

1. 右上の **デプロイ → 新しいデプロイ**
2. 歯車アイコン → **ウェブアプリ** を選択
3. 設定は以下のとおり

   | 項目 | 値 |
   |---|---|
   | 説明 | 任意（`contact form v1` など） |
   | 次のユーザーとして実行 | **自分** |
   | アクセスできるユーザー | **全員** |

   > 「アクセスできるユーザー」を **全員** にしないと、サイトからの送信が拒否されます。
   > これはスクリプトの実行権限であって、スプレッドシートが公開されるわけではありません。

4. **デプロイ** を押す
5. 初回は認証画面が出ます。「詳細」→「(プロジェクト名) に移動」→ 許可 と進んでください
6. 表示される **ウェブアプリの URL**（`https://script.google.com/macros/s/……/exec`）をコピー

## 4. サイト側に URL を設定する

`script.js` の先頭付近を書き換えます。

```js
var FORM_ENDPOINT = 'https://script.google.com/macros/s/……/exec';
var FALLBACK_MAIL = 'info@example.com';   // 送信失敗時の案内先（任意）
```

`FORM_ENDPOINT` が空のあいだは、送信しても「準備中」の案内が出るだけで実際には送信されません。
先に URL を入れてからデプロイしてください。

## 5. 動作確認

1. デプロイした `/exec` URL をブラウザで直接開く
   → `{"ok":true,"service":"hashigologi-contact","ready":true}` が表示されればスクリプトは動いています
   （`ready` が `false` の場合は `NOTIFY_TO` が未設定です）
2. サイトのフォームからテスト送信
3. スプレッドシートに行が増え、通知メールが届くことを確認

---

## 仕様

### 記録される項目

| 列 | 内容 |
|---|---|
| 受信日時 | 送信された日時 |
| 会社名 | 必須 |
| 部署・役職 | 任意 |
| お名前 | 必須 |
| メールアドレス | 必須 |
| 電話番号 | 任意 |
| お問い合わせ内容 | 任意 |
| 送信元ページ | 送信時のURL |
| 参照元 | どこから来訪したか（あれば） |
| 判定 | `通常` または `自動入力の疑い` |
| メール通知 | 通知メールの送信結果 |
| フォーム種別 | `3PL`（3PLページのフォーム）または `共通`（トップページのフォーム） |
| 月間出荷件数 | 3PLフォームのみ・任意 |
| 地域 | 3PLフォームのみ・任意 |
| 現在の倉庫坪数 | 3PLフォームのみ・任意 |
| 商材の種類 | 3PLフォームのみ・任意 |
| 想定開始時期 | 3PLフォームのみ・任意 |

「フォーム種別」以降の6列は、既に記録済みの行がずれないよう**末尾に追加**しています。

### 見出し行はスクリプトが自動で直します

項目を増やしても、**スプレッドシートを手で編集する必要はありません。**
スクリプトは送信を受けるたびに1行目を `HEADERS` と突き合わせ、違っていれば
書き直します。GASを再デプロイしたあと最初の1件が届いた時点で、
新しい列の見出しが自動で追加されます。それ以前の行は空欄のまま残ります。

ただし、**管理用に列を足す場合は必ず右端（`HEADERS` の範囲より後ろ）に**
置いてください。途中に挿入すると、スクリプトが見出しのずれとして検知し、
上書きしてしまいます。

**必須項目さえ埋まっていれば、必ず記録されます。** ボットの疑いがあるものも
捨てずに「判定」列へ印をつけるだけです。誤判定で問い合わせを失うほうが
損失が大きいためです。

「メール通知」列を見れば、通知が届かないときに原因が分かります
（`送信済み → xxx@example.com` / `送信先が未設定` / `送信失敗: ...`）。

### 通知メール

件名は `【お問い合わせ】{会社名}　{お名前} 様` です。
`Reply-To` に送信者のアドレスが入るので、**そのまま返信すれば相手に届きます**。

### スパム対策

人には見えない入力欄（ハニーポット）を1つ置いてあり、そこに値が入っている送信は
「自動入力の疑い」として印をつけます。**破棄はしません。**

> この欄の `name` とラベルは、ブラウザの自動入力に拾われない語にしてください。
> 以前 `name="address"`・ラベル「ご住所」にしていたところ、Chrome の住所
> 自動入力が値を入れてしまい、正規の送信までボット判定されていました。

エンドポイントの URL はサイトのソースを見れば分かるため、悪質な直接投稿が続く場合は
[Cloudflare Turnstile](https://developers.cloudflare.com/turnstile/) の追加が有効です。
必要になったら対応します。

### 送信数の上限

Apps Script のメール送信は無料アカウントで **1日100通** までです。
問い合わせフォームの用途では通常問題になりません。

---

## 変更するとき

- **項目を増やす** — フォームのHTML（`index.html` / `3pl/index.html`）、
  `script.js` の `payload`、`gas/form-handler.gs` の `HEADERS` と `row` を揃えて追加します。
  **`HEADERS` への追加は必ず末尾に**行ってください。途中に挿入すると
  記録済みの行が見出しとずれます。特定のページにしかない項目は、
  `script.js` 側で `optVal()` を使うと他ページのフォームを壊しません
- **スクリプトを直した** — 保存しただけでは反映されません。
  **デプロイ → デプロイを管理 → 鉛筆アイコン → バージョン「新バージョン」→ デプロイ**
  を実行してください。URL は変わりません
